github_users:
  - act-compiler

repo_description_lines_max: 2

# Optional: Add a GitHub PAT (Personal Access Token) to improve API rate limits
# Create a token at https://github.com/settings/tokens with 'public_repo' scope
# github_token: your_github_token_here

# Alternative rendering method:
# Set use_github_api to true to fetch live data from GitHub API
# This is more reliable than github-readme-stats and avoids rendering issues
use_github_api: true

github_repos:
  - act-compiler/act
  - act-compiler/taidl
  - act-compiler/taidl-artifact-micro25
